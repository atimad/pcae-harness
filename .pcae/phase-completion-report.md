# Phase 149O.20L.7O.2A Complete — RepositoryIdentity Write-Path Provisioning Gap Architecture + Remediation Proposition

**Phase ID:** 149O.20L.7O.2A
**Mode:** architecture + remediation-proposition preparation only (no Dell mutation; read-only Dell inspection permitted and used)
**Predecessor:** 149O.20L.7O.2 (RepositoryIdentity Creation on Dell + DeploymentBinding Field Resolution / Proposition Drafting — completed; RepositoryIdentity creation failed safely on a directory-permission provisioning gap; recommended this remediation-architecture phase)
**Date:** 2026-08-18
**Status:** completed
**Verdict:** `PERMISSION REMEDIATION PROPOSITION READY — ELECTION NOT INITIATED. A minimum-safe, zero-production-code-change remediation model (P-A': chmod 1770 /opt/pcae/runtime/src/.pcae — group-write + sticky bit) was derived, compared against four alternatives (P-A/P-B/P-C/P-D), attack-tested against unrestricted-delete-of-governed-state, and confirmed to leave /etc/pcae/hatp/trust-store and every HBDC-REQ-0{25..39}/topology check unaffected. HBDC-REQ-036 independently reconstructed and live-reclassified as an invocation/config mismatch (Classification A), not a blocking regression, via side-by-side live reproduction on Dell of both the mismatched and corrected Action-9 environments. No election initiated.`
**No-Go Confirmations (this phase):** `No Dell mutation of any kind (every Dell command was read-only: id/hostname/git rev-parse|status/stat/find/ls/getfacl -p/mount/uname, plus two pure-read verify_class_b_deployment_conformance() Python calls). No RepositoryIdentity created. No DeploymentBinding created. No election initiated. No CHGR published. No certification performed. No Boundary C/A or HATP activation. No .pcae/ permission change on Dell — confirmed root:pcae mode 750 unchanged before and after this session.`
**True phase-entry commit:** `fc65ca51781c36219d338194d7c4e9abb0374b29 (Phase 149O.20L.7O.2: repair fast_green field with real deselected-confirmation counts). origin/main == HEAD, 0 commits ahead, working tree clean at entry.`
**Entering state, independently reconfirmed:** `Dell source b0840e96a7ffb12308e95828aa5927c3e7c770c0 (matches phase brief); RepositoryIdentity absent; DeploymentBinding/trust-store absent (empty, no registry.json); .pcae/ root:pcae mode 750, no ACL entries beyond POSIX-mode defaults (getfacl -p confirmed live).`
**.pcae contents and ownership model reconstructed:** `17 git-tracked top-level entries (policy.toml, phase-completion-metadata.json, decision-sessions/**, publication-execution/**, etc.) — all root:pcae, mutated only via governed git commit + redeploy, not live runtime write. Cross-checked against .pcae/.gitignore's own ~34 entries (session.json, agent-lock.json, provenance-history.json, phase-reports/, and ~30 others) — every one of those is runtime-local and agent-write-required, meaning the write-path gap 7O.2 hit for repository-identity.json specifically is not narrow to that one file: all ~34 will hit the identical PermissionError on first write under the current topology.`
**RepositoryIdentity producer filesystem requirements:** `repository_identity.py read in full. tempfile.mkstemp creates its temp file at fixed mode 0600 regardless of umask; no os.chmod/os.chown call exists anywhere in the module; atomic os.replace preserves that mode. Desired final ownership/mode independently derived: pcae:pcae 0600.`
**Exact creation semantics / ACL granularity:** `Producer needs only directory-level create+rename on .pcae/ — no independent delete_child right is exercised. hatp_class_b_topology_verifier.py's own existing Linux ACL checker (_acl_grants_agent_write_linux) read directly: Linux POSIX ACLs recognize only a single "w" permission bit, with no separate add_file/delete_child right (unlike the same module's macOS NFSv4-style branch, which does distinguish them) — so on Dell's ext4/Linux filesystem, any ACL or mode grant that permits "create" also permits "delete/rename of everything in the directory," and only the sticky bit (S_ISVTX) separates the two. Neither existing HBDC write-access checker inspects S_ISVTX — a disclosed, pre-existing gap, not repaired this phase.`
**Remediation models compared:** `P-A (chmod 0770) — rejected: permits unrestricted delete/rename of all 17 root-owned governed .pcae files via directory-write-implies-delete semantics. P-A' (chmod 1770, group-write + sticky bit) — selected: same write coverage as P-A, zero production-code change, closes the delete/rename attack via kernel check_sticky() semantics. P-B (narrow POSIX ACL) — collapses to P-A's blast radius on Linux (§5), redundant with P-A' once the sticky bit is added, not selected standalone. P-C (dedicated writable subpath) — architecturally valid but requires a production code/schema migration; not the minimum-safe model for this phase. P-D (administrator pre-creates the file) — rejected per 7O.2's own §6/§10 finding (root-owned 0600 file unreadable by pcae, bypasses the canonical producer).`
**HBDC impact simulation:** `Live corrected-Action-9 re-test on Dell this session (PATH=/opt/pcae/runtime/venv/bin:/usr/bin:/bin:/usr/sbin:/sbin) confirmed sole residual HBDC-REQ-042 (no_repository_identity_present) — matches the originally expected baseline. Reviewed every HBDC-REQ-0{25..39} environment-lock and topology check source; none target .pcae/'s own mode — P-A' breaks no other requirement.`
**Protected Root distinction:** `/etc/pcae/hatp/trust-store (hatp_bootstrap.py's _LINUX_FIXED_TRUST_ROOT, line 223) confirmed structurally distinct from /opt/pcae/runtime/src/.pcae — no ancestor relationship, no remediation model in this phase touches it. Confirmed still empty live.`
**HBDC-REQ-036 reconstruction and classification:** `_check_launcher (hatp_environment_lock_verifier.py) resolves shutil.which("pcae") against the calling process's PATH only. Live side-by-side reproduction on Dell this session: (A) bare sudo -n -u pcae with no PATH override inherits sudo's own default (/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/snap/bin, excluding venv/bin) and reproduces 7O.2's exact two-residual result (HBDC-REQ-036 + HBDC-REQ-042); (B) the exact corrected canonical Action-9 invocation (149O.20L.7D.9's own PATH amendment) reproduces the single-residual baseline (HBDC-REQ-042 only). Classification: A — invocation/config mismatch, not a real host regression, reporting error, or blocking finding.`
**Remediation authority classification:** `.pcae permission remediation classified as a governed topology mutation requiring its own explicit human election and decision-session capture, per the 149O.20L.7B/7B.1/7B.2/7C Boundary-P election precedent (read directly from 7B's own record text: "proposal ≠ approval; approval ≠ provisioning"). Not authorized by any existing CHGR (chgr-541cb08c313b4f8884970172d37c5a1d scopes only to the Action-6/7-9 sequence, not .pcae's own mode/ACL state).`
**Exact remediation proposition materialized:** `Target: /opt/pcae/runtime/src/.pcae. Before: root:pcae 750, no ACL entries. After: root:pcae 1770 (group-write + sticky bit), no ACL entries (P-A' uses mode bits only). Read-back: stat -c '%U:%G %a' → expect root:pcae 1770. Rollback: chmod 750 (independently checkable via the same stat command). Exact literal chmod command deferred to the future, separately-elected execution phase's own proposition-materialization step, per this phase's own sequencing requirement (permission proposition and identity creation must not be combined in one phase).`
**Sequencing:** `Permission proposition (this phase) → election/authority-capture phase → permission-execution phase → independent-verification phase → RepositoryIdentity retry phase, using 7O.2's own already-reconstructed command. Not combined in this phase.`
**Strategic breakpoint:** `Unchanged, unreached, not begun this phase — remains gated on a first-use RepositoryIdentity + DeploymentBinding + clean, independently-verified COMPLIANT HBDC on real Dell.`

## Governance Results

- **pcae_check:** passed
- **pcae_doctor_task_memory:** warnings (pre-existing, unrelated -- historical tasks/done/ entries predating this phase, not remediated here -- outside this phase's allowed-file scope)
- **pcae_health:** healthy
- **pcae_notify_status:** telegram configured/enabled
- **pcae_permission_broker_status:** execution_unavailable, no enforcement
- **pcae_push_check:** clean at entry
- **pcae_runtime_inspect:** Observed / observe / unavailable
- **pcae_status_coherence:** coherent
- **telegram_runtime:** loaded

## Test Results

- **fast_green:** 7806 passed, 5 skipped, 0 failed, 0 errors (deselected confirmation run). Raw run with this phase's changes: 265 failed, 7806 passed, 5 skipped, 10 errors. Independently confirmed via a live `git stash -u`/pop A/B comparison this phase: with changes removed, 256 failed, 7791 passed, 5 skipped, 10 errors (all pre-existing). The remaining 9 (`test_backend_cli.py`, `test_backend_invocations.py`, `test_shell_gate.py`) were independently re-run in isolation (`-n0`, no `xdist` contention) and all 9 passed — confirmed `xdist` parallel-worker flakiness, not a regression attributable to this phase.
- **report_notification_tests:** not_applicable_this_phase
- **bootstrap_session_reporting_tests:** not_applicable_this_phase
- **new_phase_test_suite:** `tests/test_phase_149o_20l_7o_2a_repositoryidentity_write_path_provisioning_gap_architecture.py` — 24 tests, all passing.

## Recommended Next Phase

A dedicated, human-governed election/decision-session-capture phase for the P-A' (`chmod 1770 /opt/pcae/runtime/src/.pcae`) proposition materialized in this phase's §17 — mirroring `149O.20L.7B`/`7B.1`/`7B.2`/`7C`'s own Boundary-P election precedent — followed, only after that election and a dedicated execution + independent-verification phase, by a `RepositoryIdentity` creation retry using the exact, already-reconstructed command from `149O.20L.7O.2` §3/§8. Recommendation only — not initiated in this phase.
