# Phase 149O.20L.7O.1 Complete — RepositoryIdentity + DeploymentBinding First-Use Proposition Preparation Independent Verification

**Phase ID:** 149O.20L.7O.1
**Mode:** verification-only (no RepositoryIdentity/DeploymentBinding creation; no election; no CHGR; no certification; no activation)
**Predecessor:** 149O.20L.7O (RepositoryIdentity + DeploymentBinding First-Use Proposition Preparation — completed; recommended this independent-verification phase)
**Date:** 2026-08-18
**Status:** completed
**Verdict:** `VERIFIED WITH NON-BLOCKING FINDINGS — RI-D VALID. RI-D independently re-derived among RI-A/B/C/D on primary-source grounds stronger, on one point (HBDC-REQ-068), than 7O's own document cited. RepositoryIdentity creation confirmed to confer no authority and require no election. DeploymentBinding field resolution for principal_id/signer_key_id/provider_profile/authority_scope remains genuinely blocked pending a real administrative enrollment decision, independent of RI-D's validity. Two bounded, non-blocking refinements to 7O's own already-disclosed gaps were found: a pcae-OS-principal execution requirement for Dell identity creation, and an election_reference-specific evidentiary-loss mechanism within the known audit-durability gap.`
**No-Go Confirmations (this phase):** `No RepositoryIdentity created. No DeploymentBinding created. No producer create/rotate/revoke invoked against any real trust store. No human election initiated. No CHGR published. No HMIC certification performed. Boundary C/A and HATP activation not begun. All Dell access strictly read-only (git rev-parse, ls, cat, id, sudo -n true — no write verb). The only real create_deployment_binding()/ensure_repository_identity() calls this phase were against fully synthetic, disposable local tempdirs, deleted at the end of each run. No raw git commit/push used in this repository — governed pcae CLI lifecycle only.`
**True phase-entry commit:** `d4d5614d (Phase 149O.20L.7O: sync active task allowed-file list). origin/main == HEAD, 0 commits ahead, working tree clean at entry.`
**Method:** `7O's own report was read only after this document's independent reconstruction (§3-§15) was substantially complete, per this phase's own instruction not to trust it as an oracle.`
**RepositoryIdentity source reconstruction:** `repository_identity.py read in full, independently: closed 3-field schema, atomic mkstemp+fsync+os.replace write, symlink rejection at target and parent, fail-closed malformed-file handling (never auto-repaired). Module imports nothing HATP/authority-related — verified by import inspection.`
**UUID generation proof:** `_generate_repository_identity() uses bare uuid.uuid4(), no caller-supplied value, no seed, no persist-free preview mode — confirmed by direct read of the full module surface (no such parameter exists anywhere).`
**Idempotency proof:** `Disposable tempdir: read -> None; ensure -> generates; ensure again -> identical UUID returned. File mode 0600, owned by the invoking user/group (relevant to §10's Dell-topology finding).`
**Partial-failure proof:** `Four disposable attacks: write-fails-after-generation (PermissionError, no file created, safe retry generates fresh UUID since nothing was persisted); malformed-JSON (fails closed, never auto-repaired, confirmed on retry too); unknown-extra-field (rejected, strict schema); symlink attack (refused). No scenario produces multiple visible UUIDs for one repository.`
**Authority-conferral consumer inventory:** `Every real importer of repository_identity enumerated (hatp_class_b_conformance, hatp_deployment_binding_admin, hatp_mandatory_certification, hatp_mandatory_cutover, hatp_signing_ceremony, hatp_ag_authority, hatp_rollback_consumption, human_approval_trusted_provenance, commands/init.py) and read. No consumer treats identity's mere existence as sufficient to grant authority, satisfy a permission check, or change runtime capability — every consumer either uses identity as a lookup key into a separately-gated store, or uses its absence to select a specific fail-closed reason string.`
**HATP-REQ-048/051 and a stronger finding (HBDC-REQ-068):** `HATP-REQ-048 ("MAY generate... without requiring human approval") and HATP-REQ-051 read directly. A stronger, more direct primary-source statement independently located this phase, not cited by 7O: HBDC-REQ-068 — "Repository identity (Layer 1) creation is not itself gated by HBDC-REQ-056..066's election requirement... repository-identity creation confers no authority and needs no approval." This is normative SHALL-equivalent text directly settling this phase's central question.`
**Governance classification:** `A — Non-authority descriptive/bootstrap state, no human election required (HBDC-REQ-068 explicit). Distinguished from "ungoverned": still a real, dedicated, independently-verified phase requirement (§16 of the phase doc).`
**Human-owner vs. PCAE-internal governance:** `Kept explicitly distinct — real-world Dell ownership does not answer PCAE's own election-requirement question (settled independently by HBDC-REQ-068), and PCAE governance does not substitute for real OS-principal write permission on Dell's actual filesystem.`
**Filesystem trust / root-owned topology (live Dell read):** `/opt/pcae/runtime/src confirmed root:pcae 0750; codex (uid 1003) confirmed NOT a member of group pcae, confirmed unable to read the path without sudo. Path independently confirmed: /opt/pcae/runtime/src/.pcae/repository-identity.json. New finding: a naive identity-creation run as bare root (rather than as the pcae principal) would produce a 0600 root-owned file unreadable by the pcae principal every downstream consumer runs as — a concrete requirement for the future execution phase, not previously named this explicitly.`
**Git cleanliness:** `.pcae/.gitignore line 4 lists repository-identity.json verbatim — confirmed directly, not inferred.`
**HMIC digest consequence:** `derive_implementation_scope_digest() before/after RepositoryIdentity creation in a disposable copy of the 30-frozen-file tree: identical (65ff8ab06b5cd7feb2505742cfbb112ffd386c5b2cf34c2d7f3446d92afe15b8), and this value exactly matches the deployed digest — independently reconfirming that value too, since source currentness (below) guarantees zero frozen-file drift.`
**HBDC before/after-identity disposable result:** `_check_deployment_identity() isolated: before -> no_repository_identity_present; after (identity present, no binding) -> no_active_deployment_binding_matches_repository_and_root. satisfied remains False both times — exact vocabulary independently confirmed.`
**Full HBDC aggregator simulation limitation:** `verify_class_b_deployment_conformance() in a disposable temp repo: 20/34 checks fail — all topology/environment-lock checks inherently tied to real Dell provisioning, not simulable off-host. Only HBDC-REQ-022 (Model-A) and HBDC-REQ-042 (identity/binding) produce meaningful disposable results — independently confirmed, not merely assumed.`
**RI-A/B/C/D independent comparison:** `RI-A rejected (unknown repository_id at election time, no compensating benefit). RI-B rejected (would require new production code, out of scope). RI-C rejected (HBDC-REQ-068 already settles no election is needed — a dedicated one would be redundant ceremony). RI-D selected independently on authority-precision/failure-isolation/auditability grounds, matching 7O's own selection from a fresh comparison.`
**DeploymentBinding field resolution:** `9-field schema independently read from hatp_bootstrap.DeploymentBinding. repository_id/valid_from bound by generation rule + structural equality invariant (create_deployment_binding has no repository_id parameter at all). canonical_deployment_root resolved exactly (/opt/pcae/runtime/src). status/revoked_at confirmed "active"/None on creation. principal_id/signer_key_id/provider_profile/authority_scope confirmed unresolvable — live Dell read confirms the production trust-store registry.json is completely absent (zero enrolled principals/signers/authorities/bindings), so no existing value exists to reference even beyond the schema's own silence on cross-validation. Field-resolution verdict: BINDING PROPOSITION STILL BLOCKED ON FIELD RESOLUTION, independent of RI-D's validity.`
**Disposable full-sequence simulation:** `Fresh script (not reused from 7O's own): preview before identity -> RepositoryIdentityMissingError; ensure identity (repeat -> same UUID); preview after identity -> WOULD_CREATE; create -> CREATED; load via HATPTrustStore(_test_only_root=...) -> active; deployment_binding_matches() -> True; retry create -> ALREADY_SATISFIED. All behaved exactly as production code's own signatures/docstrings specify.`
**Audit-durability gap — reproduced by execution, refined:** `Reproduced by actually monkeypatching append_provenance_event to raise: binding becomes durably active in the registry despite the caller receiving an uncaught RuntimeError. New refinement not named by 7O: _COMPARED_AUTHORITY_FIELDS excludes election_reference, so a retry after such a failure can silently return ALREADY_SATISFIED without ever recovering the original creation's election linkage — a narrower, permanent evidentiary gap than "the whole audit trail is lost" (current authority state itself remains always exactly reconstructable). Verdict: NON-BLOCKING FOR FIRST USE, conditional on an explicit read-back-and-reconcile procedure before any retry.`
**Timestamp parser / HMIC-REQ-103 / HMIC-REQ-063:** `All independently reconfirmed out of scope for first creation — timestamp gap is read-path-only (producer always emits strict grammar, admin-protected write path); HMIC-REQ-103 concerns pcae push/Permission Broker consumption (unrelated subsystem); HMIC-REQ-063 concerns executed-code/import-shadowing attestation (certification-time concern, not binding-creation).`
**Fresh read-only Dell state:** `Candidate SHA b0840e96a7ffb12308e95828aa5927c3e7c770c0 (exact match to entering state); RepositoryIdentity absent; DeploymentBinding absent; trust-store empty, root:pcae 0750. No write command issued.`
**Source currentness:** `git diff --stat b0840e96a7ffb12308e95828aa5927c3e7c770c0..HEAD -- src/pcae/ returns empty — zero authority-bearing drift. Source remains current; no redeployment required.`
**CHGR scope — independently re-read:** `chgr-0e37ed1340b14311826722c4dbf3e856 condition 6 (RepositoryIdentity not named in its exclusion list — consistent with needing no election). chgr-71bd24f9d3d742d6baac772e480fc876 condition 7 ("No RepositoryIdentity creation authorized... on Dell or elsewhere") and condition 17 ("First-use transition... remains separately gated"). Neither CHGR is authority for the mutation; condition 7's prohibition does not, by itself, prove an election is required for identity specifically — that is independently settled by HBDC-REQ-068, treated as controlling since it is the more specific, more directly on-point normative text.`
**Future phase decomposition — independently assessed correct:** `7O's own §10 decomposition assessed and confirmed correctly shaped, with two concrete additions folded in: (1) identity-creation must execute as the pcae OS principal specifically; (2) the enrollment-decision prerequisite for principal_id/signer_key_id/provider_profile/authority_scope must be resolved as part of the proposition-drafting step, since no existing registry entry exists to reference.`
**Strategic breakpoint:** `Preserved unchanged, not reached this phase — after first-use DeploymentBinding + HBDC reaches independently-verified COMPLIANT on real Dell, pause before Boundary C.`
**Tests:** `No new test module added (verification-only, no production code changed). Verification performed via fresh disposable Python scripts (shown inline in the phase report, not committed as scratch artifacts) and live read-only Dell SSH commands, both reproduced verbatim in docs/PHASE_149O_20L_7O_1_REPOSITORYIDENTITY_DEPLOYMENTBINDING_FIRST_USE_PROPOSITION_PREPARATION_INDEPENDENT_VERIFICATION.md.`
**Governance results:** `pcae_health: healthy. pcae_check: passed. pcae_status_coherence: coherent. pcae_doctor_task_memory: warnings (pre-existing, unrelated). pcae_runtime_inspect: Observed / observe / unavailable. pcae_notify_status: Telegram configured/enabled. pcae_phase_report_reconcile(149O.20L.7O): reconciled, mutation none.`
**Commits:** `See .pcae/phase-completion-metadata.json phase_commits for the authoritative hash list.`
**Pushed status:** `staged pending push; pcae push to follow immediately after staging.`
**origin/main..HEAD:** `to be recorded post-push.`
**Recommended next phase:** `149O.20L.7O.2 -- RepositoryIdentity Creation on Dell (unelected, administrative, pcae-principal-executed) + Exact DeploymentBinding Proposition Drafting. Recommendation only -- not initiated in this phase.`
