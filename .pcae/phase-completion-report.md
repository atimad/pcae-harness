# Phase 149O.20L.7O Complete — RepositoryIdentity + DeploymentBinding First-Use Proposition Preparation

**Phase ID:** 149O.20L.7O
**Mode:** proposition preparation / architecture materialization only (no election; no RepositoryIdentity/DeploymentBinding creation; no CHGR; no certification; no activation)
**Predecessor:** 149O.20L.7N.5 (Dell Current-Source Redeployment Independent Verification — completed; verdict INDEPENDENTLY VERIFIED — CURRENT SOURCE DEPLOYMENT COMPLETE; recommended this preparation phase)
**Date:** 2026-08-18
**Status:** completed
**Verdict:** `FIRST-USE ARCHITECTURE REQUIRES SEPARATE IDENTITY TRANSITION — narrowly: RI-D (unelected RepositoryIdentity-creation prerequisite, then exactly one election for DeploymentBinding with full field precision) is the selected, evidence-supported model, but this phase's own governing scope forbids real RepositoryIdentity creation, so RI-D's full-precision benefit is deferred to an explicitly-inserted future sub-phase (149O.20L.7O.2), not abandoned. Not the three-election RI-C model — RepositoryIdentity creation still requires no election of its own.`
**No-Go Confirmations (this phase):** `No RepositoryIdentity created (real Dell/production path). No DeploymentBinding created. No producer create/rotate/revoke invoked against any real trust store. No human election initiated. No CHGR published. No HMIC certification performed. Boundary C/A and HATP activation not begun. All Dell access strictly read-only. The only real create_deployment_binding()/ensure_repository_identity() calls this phase were against fully synthetic, disposable local tempdirs, deleted at the end of the run. No raw git commit/push used in this repository — governed pcae CLI lifecycle only.`
**True phase-entry commit:** `4bb04018 (Phase 149O.20L.7N.5: sync phase_commits list and task allowed-file list). origin/main == HEAD, 0 commits ahead, working tree clean at entry.`
**Architecture reconstructed from primary source:** `repository_identity.py (ensure_repository_identity/_generate_repository_identity, uuid.uuid4()-only, no preview mode, idempotent); hatp_bootstrap.py (DeploymentBinding 9-field schema, deployment_binding_matches, HATPTrustStore read-only); hatp_deployment_binding_admin.py (create/rotate/revoke + preview_create_deployment_binding, _resolve_repository_id requires pre-existing identity, never calls ensure_repository_identity as a side effect); hatp_class_b_conformance.py (_check_deployment_identity / HBDC-REQ-042); hatp_mandatory_certification.py (own RepositoryIdentity dependency, separate from binding); HATP_CLASS_B_DEPLOYMENT_CONTRACT.md (HBDC-REQ-042/064/065/066/069). All read directly this session, not inherited from 149O.20L.7M's prose.`
**Repository-ID Preview Problem:** `preview_create_deployment_binding() confirmed, by direct source read, to call _resolve_repository_id() first, which raises RepositoryIdentityMissingError if no RepositoryIdentity is already persisted — no persist-free preview capability exists anywhere in current production code. Reproduced exactly in the disposable local simulation (below).`
**Disposable local simulation:** `Synthetic tempfile.mkdtemp() repository root + trust store (never the real Protected Root, never this repository's own .pcae/), using the producer's own private _protected_root= test-only seam. Sequence: (1) preview before identity exists -> RepositoryIdentityMissingError, exact reproduction of the Dell-today constraint; (2) ensure_repository_identity() -> synthetic identity generated; (3) called again -> identical value returned, idempotency confirmed; (4) preview_create_deployment_binding() now succeeds -> WOULD_CREATE, exact candidate shape shown; (5) create_deployment_binding() -> CREATED, read back; (6) deployment_binding_matches() (the exact HBDC-REQ-042 logic) run directly against the synthetic pair -> True; (7) retry with identical authority fields -> ALREADY_SATISFIED, same valid_from (no silent refresh). Both tempdirs deleted at the end.`
**Read-only Dell preview (reachable, SSH):** `sudo -n git -C /opt/pcae/runtime/src rev-parse HEAD -> b0840e96a7ffb12308e95828aa5927c3e7c770c0 (exact match to entering state); git status --porcelain=v1 empty; symbolic-ref -q HEAD exit 1 (detached). sudo -n ls .pcae/repository-identity.json -> absent. sudo -n ls /etc/pcae/hatp/trust-store -> exists, empty, root:pcae 0750. sudo -n cat registry.json -> absent. Producer module imported read-only (hasattr create_deployment_binding/preview_create_deployment_binding -> True/True); admin script --help only (no create/rotate/revoke invoked).`
**HBDC diagnostic (independent, live, corrected identity):** `An initial invocation as codex/root via bare sudo -n produced spurious extra environment-lock failures (wrong OS identity for the check's own "agent owns/writes" comparisons) — diagnosed and corrected. Re-run as sudo -n -u pcae, from /opt/pcae/runtime/src, with PATH including the venv's bin/ (matching HBDC-REQ-036's shutil.which("pcae") launcher check): NON_COMPLIANT, 34 total checks, exactly one residual (HBDC-REQ-042 / no_repository_identity_present) — matches 149O.20L.7N.5's own finding exactly, no drift.`
**RI-model selection:** `RI-A (rule-only election, unknown repository_id at election time) — rejected, strictly less precise than RI-D with no compensating benefit. RI-B (build persist-free preview capability first) — rejected, out of this phase's scope (would require new, unauthorized production code). RI-C (three-transition model: separate election solely for identity creation) — rejected, unnecessary election overhead: RepositoryIdentity creation independently reconfirmed to confer no authority whatsoever by itself (HATP-REQ-048/051 docstring text), so a dedicated election for it adds governance overhead with zero precision benefit RI-D does not already deliver. RI-D (unelected identity-creation prerequisite, then one election for DeploymentBinding with full field precision) — SELECTED, on evidence (§6 of the phase doc), not phase-count minimization.`
**Deviation from 149O.20L.7M disclosed:** `7M's own phase sketch (§48 step 6) would have had this very phase create the real Dell RepositoryIdentity to realize RI-D's full precision immediately. This phase's own governing prompt explicitly forbids that ("must not create RepositoryIdentity"). Consequence: RI-D's full-precision benefit is deferred to an explicitly-inserted future sub-phase (149O.20L.7O.2), not abandoned — the revised decomposition (phase doc §10) names this explicitly rather than silently absorbing the gap.`
**DeploymentBinding field resolution:** `canonical_deployment_root resolved exactly (/opt/pcae/runtime/src — now read-only-resolvable since source is deployed, unlike at 7M's time). repository_id/valid_from bound by generation rule + a downstream equality invariant the producer already enforces structurally (create_deployment_binding() has no repository_id parameter at all). status/revoked_at confirmed "active"/None on first creation. principal_id/signer_key_id/provider_profile/authority_scope explicitly left unresolved — no canonical derivation formula or registry exists anywhere in this repository for any of the four (HBDC-REQ-058, 149O.20L.7H finding); must be a genuine future administrative enrollment decision, not invented here.`
**Audit-durability gap classification:** `Carried-forward finding (149O.20L.7J §17, re-confirmed present in current source): create_deployment_binding can durably mutate the trust store while an unrelated audit-emission exception propagates uncaught. Classified ACCEPTABLE NON-BLOCKING FIRST-USE RISK, conditional on an immediate post-mutation audit-presence re-verification discipline in the future execution phase — reasoned explicitly (first binding, independently re-verifiable regardless of audit-record presence, blocking entirely would leave HBDC permanently NON_COMPLIANT), not carried forward unexamined.`
**HMIC-digest scope proof:** `RepositoryIdentity bytes independently proven outside the 30-member HMIC digest scope: .pcae/repository-identity.json is git-ignored (.pcae/.gitignore line 4), never enters the tracked tree implementation_scope_digest walks — proven by the same tracked-file-enumeration mechanism that scopes the digest at all, not assumed. DeploymentBinding registry proven further outside scope: lives entirely outside this repository's filesystem tree, at /etc/pcae/hatp/trust-store.`
**CHGR condition text re-read directly:** `chgr-0e37ed1340b14311826722c4dbf3e856 condition 6 ("No venv reinstall, no wrapper mutation, no DeploymentBinding, ... without a fresh, separate election") and chgr-71bd24f9d3d742d6baac772e480fc876 condition 17 ("First-use transition (RepositoryIdentity + DeploymentBinding) remains separately gated") both read directly from their JSON records this session. Neither authorizes RepositoryIdentity or DeploymentBinding creation. A new, dedicated CHGR is required for the future DeploymentBinding election — no reuse of either existing record.`
**Revised future phase decomposition:** `149O.20L.7O.1 (independent verification of this phase) -> 7O.2 (RepositoryIdentity creation, unelected, + exact DeploymentBinding proposition drafting) -> 7O.3 (independent verification of that exact proposition) -> 7O.4 (human election + CHGR) -> 7O.5 (first-use execution + HBDC re-adjudication) -> 7O.6 (independent real-host verification) -> 7O.7 (Boundary-C preparation, only after 7O.6 confirms COMPLIANT).`
**Tests:** `Not applicable — documentation-only phase, zero src/pcae/** changes. Verification performed via a disposable, uncommitted local simulation script and live read-only Dell SSH commands, both reproduced verbatim in the phase report (docs/PHASE_149O_20L_7O_REPOSITORYIDENTITY_DEPLOYMENTBINDING_FIRST_USE_PROPOSITION_PREPARATION.md).`
**Governance results:** `pcae_health: healthy. pcae_check: passed. pcae_status_coherence: coherent. pcae_doctor_task_memory: warnings (pre-existing, unrelated). pcae_runtime_inspect: Observed / observe / unavailable. pcae_notify_status: Telegram configured/enabled. pcae_phase_report_reconcile(149O.20L.7N.5): reconciled, mutation none.`
**Commits:** `(finalized below — see .pcae/phase-completion-metadata.json phase_commits for the authoritative hash list synced after commit.)`
**Pushed status:** `(finalized below — see phase-completion-metadata.json.)`
**origin/main..HEAD:** `(finalized below — see phase-completion-metadata.json.)`
**Recommended next phase:** `149O.20L.7O.1 -- RepositoryIdentity + DeploymentBinding First-Use Proposition Preparation Independent Verification. Recommendation only -- not initiated in this phase.`
