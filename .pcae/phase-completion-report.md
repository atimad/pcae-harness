# Phase 149O.20L.7O.2A.1 Complete — RepositoryIdentity Write-Path Remediation Proposition Independent Verification

**Phase ID:** 149O.20L.7O.2A.1
**Mode:** independent verification only (no Dell mutation; no Dell session opened this phase at all)
**Predecessor:** 149O.20L.7O.2A (RepositoryIdentity Write-Path Provisioning Gap Architecture + Remediation Proposition — completed; materialized the P-A' `chmod 1770` proposition, recommended a dedicated independent-verification/election phase next)
**Date:** 2026-08-18
**Status:** completed
**Verdict:** `INDEPENDENTLY VERIFIED — P-A' REMEDIATION PROPOSITION READY FOR HUMAN ELECTION, WITH ONE REQUIRED DISCLOSED CORRECTION. P-A' (chmod 1770) reconfirmed as the minimum-safe model via direct production-source reading and reference-verified Linux kernel/POSIX sticky-bit semantics. Found, independently, that 7O.2A's own claim of covering the "full write-required inventory" is inaccurate for one artifact — architecture-history.json (git-tracked, root-owned, but also written in place by write_architecture_history_snapshot's non-atomic open("w") — not fixed by a directory-mode-only remediation). This does not change the recommended command, read-back, or rollback, and does not favor any alternative model — it is a scope-accuracy correction the election materials must carry forward.`
**No-Go Confirmations (this phase):** `Zero Dell commands issued this phase (no Dell session opened at all — unlike 7O.2A). No RepositoryIdentity created. No DeploymentBinding created. No election initiated. No CHGR published. No certification performed. No Boundary C/A or HATP activation. No chmod/setfacl issued anywhere, including no macOS sticky-bit simulation (explicitly declined by the user for this phase — sticky-bit semantics cited by primary-source reference instead).`
**True phase-entry commit:** `63e2f740824cab602f3d44239ad626350a8b337c (Phase 149O.20L.7O.2A: repair pcae_push_check/pushed_status trust fields post-push). git status clean, pcae check passed, pcae push check clean nothing_to_push at entry.`
**Evidence-tier convention:** `Every claim tagged DIRECTLY SOURCE-VERIFIED (read from the actual production module this session), REFERENCE-VERIFIED FROM PRIMARY LINUX/POSIX SOURCE NOT EMPIRICALLY EXECUTED THIS PHASE (Linux kernel check_sticky()/POSIX sticky-directory semantics — §8/§9 of the phase doc), or REPOSITORY-LOCAL/DISPOSABLE TESTED (platform-independent POSIX open() behavior, verified against real primitives this session).`
**Independent artifact-inventory correction:** `.pcae/.gitignore has 39 entries, not 7O.2A's transcribed "34" — the omitted entry is architecture-history.json, which is simultaneously git-tracked (root-owned, part of 7O.2A's own 17-entry "administrator-controlled" baseline) and gitignored (declared runtime-local). Its producer, write_architecture_history_snapshot (architecture.py:112-131, called from pipeline.py/commands/session.py/commands/architecture.py — the same governed hot path ensure_repository_identity sits on), uses a direct truncating target.open("w"), not the atomic mkstemp+os.replace idiom repository_identity.py uses. Consequence: P-A''s directory-mode-only chmod does not fix this one artifact's write path (its own file mode, 0640 root-owned, is unaffected by any directory-level remediation) — though it correctly covers the other 38 declared write-required artifacts, independently reconfirmed (e.g. agent-lock.json's exclusive-create open("x", ...) IS fixed by P-A').`
**Name-squatting / idempotent-ensure surface traced:** `ensure_repository_identity() has no ownership check before trusting an existing, schema-valid identity file — confirmed by direct source read and by a disposable tmp_path fixture test. Non-blocking: pre-existing to the idempotent-ensure design (not introduced by P-A'), and the only real trust boundary (DeploymentBinding) sits entirely outside .pcae/, confirmed via direct read of hatp_bootstrap.py's _LINUX_FIXED_TRUST_ROOT.`
**Sticky-bit protection reconfirmed (reference-verified, not empirically executed):** `Linux kernel check_sticky()/POSIX sticky-directory semantics — the same, unmodified mechanism governing /tmp's own historical 1777 mode on every mainstream Linux distribution — block pcae from unlink/rename/rename-replace of any of the 17 root-owned governed .pcae entries under P-A', while still permitting pcae to create new entries. All 17 entries additionally protected by their own unaffected 0640 file mode (two independent, stacked protections).`
**P-B/P-C/P-D re-evaluated:** `P-B (ACL) reconfirmed redundant with P-A' on Linux — _acl_grants_agent_write_linux read directly this session, confirms POSIX ACLs carry only a single "w" bit, no create/delete-separating right, same conclusion as 7O.2A's own §5/§9. P-C (dedicated subpath) reconfirmed to relocate, not solve, the newly-found architecture-history.json gap — still requires a production-code/schema migration. P-D reconfirmed rejected (bypasses the canonical producer, wrong resulting ownership).`
**HBDC/Protected Root/HMIC digest reconfirmed unaffected:** `Every registered HBDC-REQ-0xx/Class-B topology check function read directly this session — none targets .pcae/'s own mode; no S_ISVTX reference exists anywhere in src/pcae (confirmed via direct grep). _LINUX_FIXED_TRUST_ROOT = /etc/pcae/hatp/trust-store, structurally distinct from .pcae/. derive_implementation_scope_digest's frozen scope is a source-file list (repository_identity.py, hatp_class_b_conformance.py, etc.), not a .pcae-runtime-path list — unaffected by RepositoryIdentity creation or any .pcae permission change.`
**Election precedent independently confirmed:** `149O.20L.7B/7B.1/7B.2's own doc files and the git-tracked class-b-boundary-p-provisioning-authorization authority-evaluation template confirmed to exist in this working tree via direct file listing — not merely cited from 7O.2A's characterization. Reconfirms .pcae permission remediation requires its own explicit human election.`
**Election proposition completeness checked:** `Exact host/path/before/after/reason/security-properties/command/read-back/rollback/exclusions all present per 7O.2A §17, independently re-checked against the doc text this session. One gap: the proposition's "covers the full write-required inventory" claim must be corrected in the election materials before 149O.20L.7O.2A.2 presents this as complete.`
**Strategic breakpoint:** `Unchanged, unreached, not begun this phase — remains gated on a first-use RepositoryIdentity + DeploymentBinding + clean, independently-verified COMPLIANT HBDC on real Dell.`

## Governance Results

- **pcae_check:** passed
- **pcae_doctor_task_memory:** warnings (pre-existing, unrelated -- historical tasks/done/ entries predating this phase, not remediated here -- outside this phase's allowed-file scope)
- **pcae_health:** healthy
- **pcae_notify_status:** telegram configured/enabled
- **pcae_permission_broker_status:** execution_unavailable, no enforcement
- **pcae_push_check:** clean
- **pcae_runtime_inspect:** Observed / observe / unavailable
- **pcae_status_coherence:** coherent
- **telegram_runtime:** loaded

## Test Results

- **fast_green:** 257 failed, 7831 passed, 5 skipped, 9 errors (full unfiltered run, this phase's changes present). Independently confirmed via a live `git stash -u`/pop A/B comparison this phase: with changes removed, 257 failed, 7814 passed, 5 skipped, 9 errors — identical failure/error counts in both runs (the +17 passed with changes present are exactly this phase's own new test file's 17 tests), confirming zero regressions attributable to this phase's doc+test-only changes.
- **report_notification_tests:** not_applicable_this_phase
- **bootstrap_session_reporting_tests:** not_applicable_this_phase
- **new_phase_test_suite:** `tests/test_phase_149o_20l_7o_2a_1_repositoryidentity_write_path_remediation_proposition_independent_verification.py` — 17 tests, all passing. `tests/test_phase_149o_20l_7o_2a_repositoryidentity_write_path_provisioning_gap_architecture.py` (pre-existing, from 7O.2A) re-run unmodified — 24 tests, still all passing.

## Recommended Next Phase

`149O.20L.7O.2A.2` — RepositoryIdentity Write-Path Remediation Human Election + CHGR Publication. Must carry forward this phase's disclosed correction (P-A' correctly fixes 38 of the 39 declared write-required `.pcae` artifacts; `architecture-history.json` needs a separate, not-yet-designed producer fix), re-check proposition currentness with a fresh Dell read, present `APPROVE`/`DECLINE`/`AMEND`, publish CHGR, and perform zero Dell mutation. Then `7O.2A.3` (authority independent verification), `7O.2A.4` (`chmod 1770` execution), `7O.2A.5` (independent real-host verification), and only after a clean `7O.2A.5`, a `RepositoryIdentity` creation retry under a new phase. Recommendation only — not initiated in this phase.
