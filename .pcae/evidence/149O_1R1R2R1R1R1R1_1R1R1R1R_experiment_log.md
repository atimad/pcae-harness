# Experiment Log — Phase 149O.20L.7O.3W.1R.2B.1R.1.1R.30R.5R.2.1R.1R.2R.1R.1R.1R.1.1R.1R.1R.1R

D0 = `e6bd2c718eca485104da8638ed8122035f692ed3`
Environment: `.venv/bin/python` = Python 3.9.6, pytest 8.4.2, pytest-xdist 3.8.0.
Budget: max 30 invocations OR 60 minutes experimental execution time, whichever first.
Victim module: `tests/test_phase_149o_20l_7o_3w_1r_2b_1r_1_1r_30r_3_4_merged_rhamp_mechanism.py`
Victim fixture chain: `Rig.__init__` -> `enroll_principal_via_pawa` (`src/pcae/core/hpac_protected_admin_writer.py:1095`) -> `HumanPrincipalRegistryStore.__init__` (`src/pcae/core/human_principal_registry.py:260`) -> `isinstance(root, HPACStoreAuthority)` evaluates False against a live `HPACStoreAuthority` instance (confirmed from surviving traceback in `full_sweep_reproduction_at_head.log` lines 718-746).

Budget clock starts now.

| # | Time (start-end, wall) | Command | Purpose | Result | Evidence |
|---|---|---|---|---|---|
| 1 | 08:22:46-08:22:50 | `.venv/bin/python -m pytest -q -p no:cacheprovider tests/test_phase_149o_20l_7o_3w_1r_2b_1r_1_1r_30r_3_4_merged_rhamp_mechanism.py` | Victim-alone baseline at D0 | `125 passed in 2.82s` — clean, confirms victim module has no deterministic defect of its own | `/tmp/exp1_victim_alone.log` |
| 2 | 08:23:05-08:23:50 | `.venv/bin/python -m pytest -q -p no:cacheprovider tests/test_cltr_authority_136a*.py tests/test_cltr_rehearsal_135u_independent_verification.py tests/<victim>` | Candidate composition 1: the 15 `importlib.reload(...)`-using CLTR-authority test files that sort alphabetically before the victim (only files in the whole suite found, by exhaustive grep, to call `importlib.reload`/mutate `sys.modules` at all) + victim, single process | `42 failed, 2932 passed, 1 skipped in 38.69s` — **victim module: 0 failures/errors, fully clean.** The 42 failures are an unrelated wheel/sdist packaging-artifact cluster (`test_136a[n-w]_*_wheel_contains_*`/`*_sdist_includes_*`/`*_isolated_install_*`), not the RHAMP contamination signature. **Hypothesis falsified: CLTR-authority reload tests are not the trigger.** | `/tmp/exp2_cltr_plus_victim.log` |
| 3 | 08:24:34-08:30:53 | `.venv/bin/python -m pytest -q -p no:cacheprovider $(cat clean_isolated_files.txt)` (55 files: every file the predecessor's reconciliation found "clean in isolation" — i.e. the full thematic RHAMP/PAWA/HATP/CLTR self-cluster, including the victim and all its `.30R.3.*`/`.30R.4R.*`/`.30R.5R.*` siblings) | Candidate composition 2: does the contamination reproduce within this narrow, thematically-related 55-file cluster alone, without the other ~500 files in the suite? | `2148 passed, 1 skipped in 370.64s` — **fully clean, zero failures/errors anywhere in the cluster.** **Hypothesis falsified: the trigger is not contained within the RHAMP/PAWA/HATP/CLTR thematic self-cluster; it requires some file(s) outside this 55-file set.** | `/tmp/exp3_cluster55.log` |
| 4 | 08:31:49 (~1s) | `.venv/bin/python -m pytest -q -p no:cacheprovider tests/..._configured_agent_identity_threading_repair.py tests/..._1r_1_independent_verification_configured_agent_identity_threading_repair.py` | Targeted current regression for the configured-agent-identity repair (phase §21) | `8 failed, 60 passed, 3 skipped` — 1 of the 8 was self-inflicted (this phase's own task-scope not yet widened, causing `pcae health` to report unhealthy when the test shelled out to it) | `/tmp` transcript (terminal only, not separately saved — superseded by invocation 5) |
| 5 | 08:32:0x (~1.3s) | same as #4, re-run after widening this task's allowed-file globs | Re-verify #4 after removing the self-inflicted cause | `7 failed, 61 passed, 3 skipped` — remaining 7 are (a) 5 HISTORICAL-MOVING-AUTHORITY-pattern assertions (fixed pre-CAIR SHA vs current HEAD; break on any later legitimate commit, same anti-pattern as the two named in the phase prompt) and (b) 2 `PermissionError` reads of `/Library/Application Support/PCAE/HPAC/protected-root/.authority/*` — expected fail-closed OS-permission behavior for an unprivileged process reading protected-root files, not a regression. **No functional contradiction of the configured-agent-identity repair found.** | terminal transcript |

| 6 (attempted between #4/#5 in wall-clock terms; renumbered here for narrative order) | 08:34:18-~08:45 (aborted) | `.venv/bin/python -m pytest -q -p no:cacheprovider -m "not slow and not integration and not phase_closure" <571-file prefix> <victim>` | Candidate composition 3: the full alphabetical prefix (571 files) minus slow/integration/phase_closure-marked tests, to see whether excluding the suite's admittedly-dominant slow/subprocess tests makes a full-prefix bisection tractable within budget | Aborted (accidentally killed by an overlapping wait-script of this session's own tooling, not by budget expiry) after ~10-11 minutes of continuous execution having reached only **~7% of collection** (2 failures observed so far, neither yet identified as the victim — the victim module was not reached before abort). Extrapolating the observed rate (~7%/~10min), completing this composition would require on the order of 2.5+ hours — **infeasible within the remaining diagnostic budget regardless of the accidental kill.** Confirms: even after excluding slow/integration/phase_closure-marked tests, the near-full-suite prefix remains far too expensive to bisect with the available budget. | `/tmp/exp4_quicktier_prefix.log` |

Budget used: 5 targeted pytest invocations (of 30 max) + 1 aborted/infeasible large-scale attempt; ~6.5 minutes of cumulative pytest wall-clock execution (of 60 min max). Both constructed candidate-contamination compositions were definitively falsified. No test file anywhere in `tests/` calls `importlib.reload` or mutates `sys.modules` in a way that touches `hpac_foundation`/`HPACStoreAuthority`/`human_principal_registry`/`hpac_protected_admin_writer` (exhaustive grep, zero hits) — ruling out the most direct "test reloads the victim's dependency module" mechanism. Reproducing the trigger appears to require a large fraction of the ~572 files that sort before the victim (a single-process run of that full prefix would itself take on the order of half the original 8831s full-sweep runtime — far beyond the remaining budget to bisect further with the invocations/time available). **Diagnostic experimentation stopped here as a deliberate budget-respecting decision, not because the budget was exhausted** (5/30 invocations, ~6.5/60 minutes used) — continuing to blindly bisect the remaining ~572-file prefix without a further falsifiable hypothesis would not be a responsible use of the remaining budget. **CONTAMINATION ROOT CAUSE: UNRESOLVED.**
