# Phase 149O.20L.7O.3W.1R.2B.1R.1.1R.30R.5R.2.1R.1R.2R.1R.1R.1R.1.1R.1R.1R.1R.1R.1R.1R.1R.1R.1R.1.1R.1R.1R.1R.1R.1R.1R — Independent Verification of the N-16-5 Production Certification Authority-Path Implementation Against HPAC-PAWA-001 v1.3 — H-3 Repair

**Alias:** N16-5-H3-IV (operator readability only; the full canonical CPIPC id above is authoritative in all task state, lifecycle, reports, completion metadata, evidence, and canonical project status).

**Status:** COMPLETE.

## 0. CPIPC verification

| Check | Result |
|---|---|
| Predecessor alias | N16-5-H3-IMPL |
| Expected canonical predecessor | `149O.20L.7O.3W.1R.2B.1R.1.1R.30R.5R.2.1R.1R.2R.1R.1R.1R.1.1R.1R.1R.1R.1R.1R.1R.1R.1R.1R.1.1R.1R.1R.1R.1R.1R` |
| Candidate | predecessor + exactly one `.1R` |
| `same_series` / `same_branch` | True (series `149`, branch `O`) / True (`main`) |
| `compare(pred, cand)` | `less` — strict ordering, pred < cand |
| Direct successor | cand = pred + one `.1R`; no interposed token |
| Uniqueness / no active conflicting phase | confirmed at `pcae task transition` |
| Canonical id == proposed id | yes — alias is display-only; **NO CPIPC discrepancy** |

## 1. Frozen inputs

- **V0 (this IV's phase-entry SHA):** `ef9a3c7b5e8a2b6f44850b057b73ee99ef682ebe`
- **I_ENTRY (implementation phase-entry SHA, independently derived as parent of the first N16-5-H3-IMPL commit `a22f830e`):** `74e52d59738007c4b9f6dbeb28f83990ba82e9a8` — matches the claimed I0.
- **I_FINAL (implementation phase's last commit):** `ef9a3c7b` (finalization/metadata; last *content* commit `90ac45c5`).
- **HPAC-PAWA-001 v1.3** git blob at I_ENTRY and at HEAD: `9c816716bae2262831945ac24b1771cf79de4c55` — **byte-identical**.

## 2. Independence

This IV did not inherit the implementation phase's verdict. Truth was reconstructed from immutable git (`I_ENTRY..HEAD`), primary contract source, the production source at HEAD, an A/B worktree at `I_ENTRY`, and a fresh independent adversarial suite (`tests/test_phase_n16_5_h3_iv_independent_verification.py`, 19/0) that does not import the predecessor fixtures. The predecessor's 106-test suite was reproduced (106/0) but treated as corroboration, not proof.

## 3. Exact production diff (§8)

`git diff --name-only I_ENTRY HEAD -- src scripts` = exactly four files, **EXPECTED == ACTUAL**, no unexplained production file:

1. `scripts/hpac_certification_admin.py` (new, 130 lines — bounded `describe`/`status` only)
2. `src/pcae/core/hpac_certification_coordinator.py` (new, 475 lines — sole §38A consumer)
3. `src/pcae/core/hpac_protected_admin_writer.py` (+386 — `certification_writer` factory + §33A + `CertificationWriterHandle` + closed 5-role allowlist)
4. `src/pcae/core/human_authentication_proof.py` (+46/−6 — additive `certification_proof_subject` kwarg, OQ-1)

1031 insertions / 6 deletions. `src/pcae/core/hpac_foundation.py` (the mint primitive) **byte-unchanged**.

## 4. Contract identity (§9)

`docs/contracts`, `schemas`, `pyproject.toml` diff since `I_ENTRY` = **EMPTY**. HPAC-PAWA-001 v1.3 / HPAC-001 v2.1 / RHAMP-001 v1.0 / HBDC-001 v1.2 / HPAC-PPA-001 v1.0 / `hpac_pawa_schemas.py` all byte-unchanged.

## 5. Verification results (§§10–74)

| Property | Verdict |
|---|---|
| Production trust root | **EXISTING ROOT REUSED** — same `_PRODUCTION_WRITER_FACTORY_SEAL` object (imported from `hpac_foundation`, shared with `production_writer` / `mint_protected_presentation_evidence_writer`); no `object()` in the added code; `_mint_production_writer_capability` unchanged. **No second trust root / seal / mint primitive.** |
| `certification_writer` dedicated | **INDEPENDENTLY VERIFIED** — distinct symbol, own closed consumer inventory `frozenset({"pcae.core.hpac_certification_coordinator"})` (disjoint from `AUTHORIZED_FACTORY_CONSUMERS`); `role` constrained to exact membership in the closed five before any mint; generic `production_writer` authority NOT introduced (`HPACStoreAuthority.writer()` still raises for every non-`FIXTURE_NON_REAL` class). |
| §33 steps 1–9 | **PRESERVED** — shared `_run_recognition_sequence`; only the enumerated consumer / test-consumer sets are parameterised (per-factory), verbatim otherwise. Both `production_writer` and `certification_writer` are its only callers. |
| §33A fail-closed | **VERIFIED** — every recognition-conjunct failure and every certification-specific check (wrong consumer / category / configured identity / role / session / subject / inactive-revoked principal / inactive-revoked-unbound credential / malformed-`None`-bypass input) → the mapped `pawa_failure_code`; no capability minted. |
| Five-role allowlist | **VERIFIED** — exact `frozenset` of 5 with `assert len == 5`; gate is `in`/`not in` membership, no `startswith`/`endswith`/`match`/`search`/`fnmatch`/regex. `hpac_lifecycle_terminator` **DENY VERIFIED** (explicit `assert not-in` + `operation_scope_invalid` #16). Unknown / wildcard / prefix / near-miss **DENY VERIFIED**. |
| §39A consumer inventory | **CLOSED VERIFIED** — `hpac_certification_coordinator` / `certification_writer` absent from `cli.py`, `commands/**`, `core/agent.py`; only caller of `certification_writer` is the coordinator; admin script is standalone, not a CLI subcommand, not a console-script entry point. |
| Private test-injection seams | **NON-AUTHORITY VERIFIED** — four one-leading-underscore keyword-only seams; `_CERTIFICATION_TEST_CONSUMERS` is a single test module; guards assert no non-test module is a member / passes a seam; ambient identity and caller-string are insufficient (`test_18`/`test_19`). The full chain composes with **no** `_mint_production_writer_capability` / `_PRODUCTION_WRITER_FACTORY_SEAL` test seal (`test_100`). |
| `certification_proof_subject` | **NARROW BINDING VERIFIED** — additive kwarg on `create_canonical`; sole caller passes `self.proof_id`; if provided must `== proof.proof_id` else raise; fixture `subject == mechanism_id` path byte-unchanged; `resolve_canonical` accepts `writer_subject ∈ {mechanism_id, proof_id}` — both digest-bound immutable fields of that same proof. Not caller-controlled authority. |
| OQ-1 | additive, within v1.3 §33A/§43A subject-binding semantics; no new normative semantics. Proof-verifier capability minted `_multi_write`, spent once via `complete_multi_write` after both writes + read-back (one verification transaction, §42B/§49A). |
| One-shot / session / subject | **VERIFIED** — `CertificationWriterHandle._consumed` + foundation `_single_use`/`_spent`; second consume → `capability_stale`; wrong role/session/subject → `target_scope_invalid`; `__reduce__` raises; a second `certification_writer` call re-runs the full §33A sequence (restart-dead seal). Remint / delegation / escalation **DENIED**. |
| Principal / credential currentness | **VERIFIED** — `_validate_certification_session_binding` reads the canonical `PrincipalRecord` (active) + `CredentialRecord` (active, bound to principal) from trusted store; revoked / unresolvable → `operation_scope_invalid`. |
| Challenge / assertion / proof / counter production paths | **VERIFIED** — `open_challenge_canonical` / `record_assertion_canonical` / `create_canonical`+`record_verified_canonical` / `apply_after_verification` (via `verify_human_authentication`), each on the freshly-recognized §33A authority; forged challenge / assertion / proof / caller-supplied counter ACCEPT all rejected. |
| Presentation-evidence writer | **OUTSIDE the five-role family** — existing `mint_protected_presentation_evidence_writer` reused unchanged; coordinator consumes, does not manufacture, trusted presentation evidence. |
| Production principal issuer | `verify_human_authentication(require_real_assurance=True)` remains the **SOLE** PRODUCTION `AuthenticatedHumanPrincipal` issuer; coordinator does not construct one, does not relax `require_real_assurance` (its own default `True`, no `require_real_assurance=False` anywhere). |
| Actual Gate-5 path | **VERIFIED** — `reach_gate5_assurance` calls `verify_human_authentication` and returns the verifier-issued principal (the bounded Gate-5 assurance result) then stops. |
| Gate-5 effect termination | **VERIFIED** — coordinator's transitive import graph contains no `dispatch` / `adapter` / `runtime_authority` / `permission_broker` / `gate6-10` module (AST, `test_iv33`). |
| Deterministic non-elevation | **VERIFIED** — coordinator production status does not elevate `authority_class`; deterministic auth / presentation / mixed-non-real cannot reach REAL assurance. |
| PB / policy walls | **VERIFIED** — coordinator imports / manufactures no PB permission, policy exception, RE result, runtime capability, or `DispatchEnvelope`. |
| Ordinary-actor non-authority | **VERIFIED** — launcher / helper / ordinary verifier / Gate / runtime / agent / CLI / plugin / test fixture cannot acquire any of the five roles. |
| Forged `AuthenticatedHumanPrincipal` | **DENY VERIFIED** at the real Gate-5 boundary (`test_117` + `object.__new__` forgery rejected). |
| No new `PawaOperation` / `pawa_failure_code` / RHAMP `terminal_reason_code` / schema field / dependency | **VERIFIED** — `PawaOperation` = 6 members; `PAWA_FAILURE_CODES` = 21; RHAMP vocab unchanged; no schema field; `pyproject.toml` byte-unchanged. |
| Admin script boundary | **VERIFIED** — `describe` / `status` only; no `--role` / arbitrary consumer / `--approve` / `--yes` / `--pin` / `--protected-root` / `--fake-real` / shell escape / subprocess injection / runtime dispatch. |
| Runtime | not_implemented / Observed / observe / unavailable; 0 plugins / 0 capabilities; **first governed runtime external effect ABSENT / UNREACHABLE**. |

## 6. Regression attribution (§§60–62, §88–89)

A/B: worktree at `I_ENTRY` (`74e52d59`) vs HEAD, same 16-file reconciled guard set, `comm`-diff of FAILED node lists.

- **A failed: 23. B failed: 23. Candidate-only (attributable) regressions: 0** — the failure sets are identical.
- All failing nodes are: (a) point-in-time diff-scope guards comparing a fixed baseline against moving `HEAD`; (b) blocking-reproduction guards from `.1R.31` / `.1R.321` (the documented pre-existing trust-foundation defect); (c) two Python-3.14 `object.__new__` verifier guards. Each reproduces identically at `I_ENTRY`.
- Broader targeted (`-k certification/hpac_protected_admin/human_authentication_proof/pawa`): 1265 passed, 29 failed — all pre-existing point-in-time / blocking-reproduction guards from other completed phases (7d.8, 7n.3, 7o.2h.0, 30r.5r.2.1, f6, …), which fail *more* at `I_ENTRY` (14 in the sampled subset) — net incidental repair, zero new.
- **No `def test_` renamed / removed** across all 18 predecessor-touched test files. **No `skip` / `xfail` added** (one `skipif(os.name != "posix")` platform guard in the new predecessor suite — not a weakening).

## 7. Guard reconciliation IV (§60)

- **Pattern A** — add the exact filename `hpac_certification_coordinator.py` (never a wildcard / glob / prefix) to consumer-inventory allowlists it legitimately belongs in: the coordinator genuinely imports `hpac_foundation` / `hpac_lifecycle` / `human_authentication_proof` / `approval_presentation` / `human_principal_registry` / `hpac_verifier`, and is non-agent-importable via its own §39A guard. The `hpac_verifier` entry is correct — the coordinator imports `verify_human_authentication` *precisely so it does not bypass the verifier* (§42B / HPAC-PAWA-REQ-247).
- **Pattern B** — re-anchor completed-predecessor point-in-time guards from moving `HEAD` to their own fixed phase-completion SHAs (`90510428`, `67d542ef`, `a3b66561`, `e44becc9`, `74e52d59`) — all independently confirmed ancestors of `I_ENTRY`.
- No wildcard / glob / prefix broadening; the widen-not-weaken orientation (`new_set - old_set == {exact tuples}`, `old_set - new_set == set()`) is preserved.

## 8. Packaging (§§36–37, §84–85)

An offline wheel build was **not performable** on this host (the `hatchling` build backend is unavailable offline — environmental, not an IV defect, mirroring the predecessor's F-2 `posix_spawn` class). Packaging was verified structurally + by import graph: all three production modules are under `src/pcae/core/` (included in any wheel); `scripts/hpac_certification_admin.py` is intentionally **not** wheel-packaged (standalone, §37 / §38A, mirroring `hatp_certification_admin.py` — §85 allowance); the coordinator imports only `pcae.core.*` + stdlib, no `tests` / `pytest` (`test_64` / `test_iv34`).

## 9. Host-environment & live-state continuity (§§63–66)

- **RESOLVED:** the implementation phase and this IV ran on a host where the **real HPAC protected root is ABSENT**; every mutation test used a disposable `tmp_path`-provisioned PRODUCTION test-fixture root. Zero live protected-root access, zero live-state mutation.
- The current real `PrincipalRecord hp-8cee9b36…`, `CredentialRecord hpc-2e7bbfa0…`, counter state (generation 0), and generation-1 protected-presentation deployment were **independently verified by prior governed phases** and are **not re-read here** (root absent on this host). Their current freshness re-confirmation is a first-order task of **N16-5-FINAL-CERT** on the real host.

## 10. Verdicts (§99)

| | |
|---|---|
| PHASE ALIAS | N16-5-H3-IV |
| IMPLEMENTATION ENTRY | `74e52d59738007c4b9f6dbeb28f83990ba82e9a8` |
| IMPLEMENTATION FINAL | `ef9a3c7b` (last content commit `90ac45c5`) |
| PRODUCTION DIFF | exactly `scripts/hpac_certification_admin.py`, `src/pcae/core/hpac_certification_coordinator.py`, `src/pcae/core/hpac_protected_admin_writer.py`, `src/pcae/core/human_authentication_proof.py` |
| HPAC-PAWA-001 v1.3 | **BYTE-UNCHANGED** |
| H-3 IMPLEMENTATION CONTRACT COMPLIANCE | **VERIFIED** (§33A / §38A / §39A / §42B / §42C / §43A / §44A / §49A / §68A) |
| PRODUCTION TRUST ROOT | **EXISTING ROOT REUSED** — no second trust root |
| certification_writer / §33A / §33 steps 1–9 | **INDEPENDENTLY VERIFIED** / **INDEPENDENTLY VERIFIED** / **PRESERVED** |
| FIVE-ROLE ALLOWLIST / TERMINATOR / UNKNOWN·WILDCARD·PREFIX | **VERIFIED** / **DENY VERIFIED** / **DENY VERIFIED** |
| CONSUMER INVENTORY / TEST-SEAL DEPENDENCY / PRIVATE SEAMS | **CLOSED VERIFIED** / **ABSENT VERIFIED** / **NON-AUTHORITY VERIFIED** |
| certification_proof_subject | **NARROW BINDING VERIFIED** |
| ONE-SHOT / SESSION BINDING / SUBJECT BINDING / REMINT / DELEGATION / ROLE ESCALATION | **VERIFIED** / **VERIFIED** / **VERIFIED** / **DENIED VERIFIED** / **DENIED VERIFIED** / **DENIED VERIFIED** |
| CHALLENGE / ASSERTION / PROOF / COUNTER PRODUCTION PATH | **VERIFIED** / **VERIFIED** / **VERIFIED** / **VERIFIED** |
| PRESENTATION EVIDENCE WRITER / PRODUCTION PRINCIPAL ISSUER | **EXISTING BOUNDARY REUSED VERIFIED** / **`verify_human_authentication` VERIFIED** |
| ACTUAL GATE-5 PATH / GATE-5 EFFECT TERMINATION | **VERIFIED** / **VERIFIED** |
| ORDINARY ACTOR NON-AUTHORITY / DETERMINISTIC NON-ELEVATION | **VERIFIED** / **VERIFIED** |
| PB WALL / POLICY WALL | **VERIFIED** / **VERIFIED** |
| PACKAGE CLEAN-INSTALL PATH | **VERIFIED (structural + import-graph; offline wheel build not performable — environmental)** |
| HISTORICAL GUARD RECONCILIATION | **INDEPENDENTLY VERIFIED** (widen-not-weaken; 0 attributable regression) |
| HOST ENVIRONMENT DISTINCTION | **RESOLVED** |
| CURRENT PRINCIPAL / CREDENTIAL / COUNTER / GEN-1 DEPLOYMENT | **PRIOR VERIFIED ONLY** — current re-read deferred to N16-5-FINAL-CERT (real host); **UNCHANGED** (0 mutations this phase) |
| LIVE HOST MUTATIONS / REAL CEREMONY | **0** / **NOT PERFORMED** |
| **H-3** | **INDEPENDENTLY VERIFIED** |
| **F-5** | **DEPLOYMENT VERIFIED HISTORICALLY — CURRENT CERTIFICATION READINESS NOT YET RECONFIRMED** (defer live principal/credential/counter/gen-1 re-read to N16-5-FINAL-CERT on the real host) |
| **N-16-5** | **NOT CLOSED** |
| **N-16-6 / N-16-7** | **OPEN / UNTOUCHED** ; N-16-7 strictly last |
| RUNTIME | Observed / observe / unavailable |
| FIRST GOVERNED RUNTIME EXTERNAL EFFECT | **ABSENT / UNREACHABLE** |
| REPORTING-UX-1 | not repaired here — canonical phase ID remains authoritative |
| DELEGATED `.3` FINALIZATION / COMMIT / PUSH | **UNAUTHORIZED** (preserved) |

## 11. Required next phase (§95) — derived, NOT begun

**N16-5-FINAL-CERT** — *Final Real-Human / Genuine-YubiKey Protected-Presentation N-16-5 Certification and Closure Adjudication — After H-3 Production Authority Repair Verification*. A fresh CPIPC-valid successor id (recommended, **NOT reserved**); never a reused completed certification id. Its own explicit human authorization + its own human authentication. It must freshly revalidate the canonical human principal, credential, counter state, generation-1 protected presentation, the H-3 production authority path, and real provider/hardware availability, then run the real ceremony (protected APPROVE → real presentation evidence → genuine YubiKey getAssertion → UP + UV → real authentication → `require_real_assurance=True` → PRODUCTION `AuthenticatedHumanPrincipal` → actual Gate 5 → negative matrix → N-16-5 closure adjudication). No step of that ceremony was performed here.

## 12. Evidence

`.pcae/certification/n16_5_h3_iv_independent_verification.json` — full machine-readable IV record.
`tests/test_phase_n16_5_h3_iv_independent_verification.py` — fresh independent adversarial suite (19/0).
