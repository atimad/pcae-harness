# Phase 95T — Dry-Run Orchestration CLI
phase_name = phase_95t_orch_cli | phase_status = completed | recommended_next_phase = 95U — Orchestration End-to-End Demo
## 1. CLI Commands
pcae backend invoke artifact-only orchestration dry-run --plan <path> [--save] [--json]
pcae backend invoke artifact-only orchestration show --latest [--json]
pcae backend invoke artifact-only orchestration verify --latest [--json]
## 2. Tests (7) — all pass. Execute unavailable. Next: 95U
